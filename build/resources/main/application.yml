server:
  port: 8081

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/users_db
    username: root
    password: root

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
appDescription: "Service user"
appVersion: "1.0.0"

jwt:
  secret: ${ACCESS_TOKEN_SECRET:Zy1vNcA3rBpX7LsGt49qWeJkMmPnRtUv}

logging:
  level:
    org:
      springframework:
        security: DEBUG
